<% @collection = collection || @collection -%>
<% @am = am || @am -%>
<h4>Export CSV:</h4>
<%=link_to "Download the raw CSV zip (Users/Tweets)", "http://#{SITE_URL}/files#{@am.save_path}#{@collection.folder_name}.zip" %>